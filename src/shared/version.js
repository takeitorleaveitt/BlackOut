// The build's version, in one place so the menu badge and the version tag in
// the corner can never disagree with each other.

export const GAME_VERSION = '1.2.0';
export const GAME_VERSION_LABEL = 'Version 1.2 Beta';
